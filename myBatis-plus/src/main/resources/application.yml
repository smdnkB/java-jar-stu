

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.0.100:3306/mybatis-plus-stu?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      table-prefix:
#      配置 mybatis plus 查询数据库拼接表名字的前缀
      #table-prefix: sys_
